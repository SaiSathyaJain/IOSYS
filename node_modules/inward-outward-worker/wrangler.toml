name = "inward-outward-api"
main = "src/index.js"
compatibility_date = "2024-09-23"
compatibility_flags = [ "nodejs_compat" ]

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "inward-outward-db"
database_id = "65cdca58-f391-42fd-b314-2bdf52aa6a19"

# Environment variables (set via wrangler secret or dashboard)
# EMAIL_API_KEY - for email service (Resend recommended)

[vars]
ENVIRONMENT = "production"
BOSS_EMAIL = "sathyajain9@gmail.com"

[triggers]
crons = ["0 9 * * 6"] # Every Saturday at 09:00 AM UTC (adjust for offset if needed, Cloudflare uses UTC)

